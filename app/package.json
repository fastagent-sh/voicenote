{
  "name": "app",
  "private": true,
  "version": "0.1.10",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "bun test",
    "build": "bun test && tsc && vite build",
    "build:sidecar": "bash scripts/build-vn-sidecar.sh",
    "build:sidecar:universal": "bash scripts/build-vn-sidecar.sh universal",
    "build:sidecar:win": "powershell -ExecutionPolicy Bypass -File scripts/build-vn-sidecar.ps1",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.14",
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
